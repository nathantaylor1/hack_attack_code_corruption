using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayPhantom : MonoBehaviour
{
    bool played = false;
    // private void OnTriggerEnter2D(Collider2D other) {
    //     if (other.gameObject.layer == LayerMask.NameToLayer("Player") && !played) {
    //         played = true;
    //         PhantomController.instance.StartMoving();
    //     }
    // }
}
