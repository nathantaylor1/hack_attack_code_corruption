using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

// The only reason this exists is so it gets added first to the editor window before any other code modules
public class PlayerCodeModule : CodeModule
{
}
